{
  "events": {
    "restart": "kill-port 3000",
    "crash": "kill-port 3000"
  },
  "watch": ["src"],
  "ext": "ts, ejs",
  "exec": "node --inspect -r ts-node/register ./src/index.ts",
  "delay": "2000"
}